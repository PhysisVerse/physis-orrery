# Orrery Program Registry

## Program 1: Physis Epoch Registry

Program name:

```text
physis_epoch_registry
```

Program ID:

```text
PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE
```

Purpose:

```text
Canonical Physis epoch clock and ASTRALIS service epoch anchor.
```

Repository:

```text
https://github.com/PhysisVerse/physis-orrery
```

Verified source commit:

```text
17f0fcb35601c8fc0537d952d5a5f224a8bd1310
```

Verified source URL:

```text
https://github.com/PhysisVerse/physis-orrery/tree/17f0fcb35601c8fc0537d952d5a5f224a8bd1310
```

---

## Mainnet Program State

Mainnet program:

```text
PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE
```

ProgramData address:

```text
9qXAL9PRc9TuNW8Lk9CWyAaXHsga8gGcPZiwT4tuXUSU
```

Current upgrade authority at time of deployment/verification:

```text
wfSXjyiLAv2mmCyPBhgT5ZNaPtAenNjQ6jaanQpdJJm
```

DAO upgrade authority target:

```text
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8
```

Mainnet registry initialization status:

```text
Pending DAO execution.
```

Mainnet registry authority target:

```text
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8
```

Mainnet registry initialization should be executed by the Physis DAO through Realms, not by an individual developer wallet.

---

## Mainnet Verification State

Verifiable build status:

```text
Verified
```

Verification job:

```text
25cf187c-a3ad-499d-aeeb-ded0d6e1d4d7
```

Verification status URL:

```text
https://verify.osec.io/status/PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE
```

Verification job URL:

```text
https://verify.osec.io/job/25cf187c-a3ad-499d-aeeb-ded0d6e1d4d7
```

On-chain program hash:

```text
7d19d0556c0f081c7641a164a08c30f3991f8f7400eb8c2709ce5291a3fa46a8
```

Executable hash:

```text
7d19d0556c0f081c7641a164a08c30f3991f8f7400eb8c2709ce5291a3fa46a8
```

Verification result:

```text
The provided GitHub build matches the on-chain hash.
```

Verification upload transaction:

```text
cGyu9y6ByQapqF3SvDYqodoyf1CTCkSJEM3kvYfDLtkYCvEd3TCmU7dAct4eSeCETJevBjNKrgnsTd1CvSfYEjT
```

---

## Mainnet Metadata State

Embedded security metadata:

```text
Included through solana-security-txt.
```

GitHub security policy:

```text
.github/SECURITY.md
```

Security policy URL:

```text
https://github.com/PhysisVerse/physis-orrery/security/policy
```

Program Metadata security profile:

```text
Uploaded.
```

Program Metadata security seed:

```text
security
```

Program Metadata security PDA:

```text
3NjYQjxbgpdQFxAjJ1wwH2c3wFqtGtzD7REvAoDZwm5K
```

Program Metadata security JSON:

```text
https://cdn.jsdelivr.net/gh/PhysisVerse/physis-orrery@main/metadata/security.json
```

Program Metadata IDL profile:

```text
Uploaded.
```

Program Metadata IDL seed:

```text
idl
```

Public IDL URL:

```text
https://cdn.jsdelivr.net/gh/PhysisVerse/physis-orrery@main/idls/physis_epoch_registry.json
```

Traditional Anchor IDL status:

```text
Confirm separately with anchor idl fetch.
```

Note:

```text
Some explorers may distinguish between the traditional Anchor IDL account and the newer Program Metadata IDL account. Program Metadata IDL upload does not necessarily cause every explorer's Anchor IDL page to update immediately.
```

---

## Physis DAO / Realms Authority Map

Realm:

```text
DsWWtZrqXBcqTTPoEyFH793Euq82r95CuXWTwqo3JZur
```

Realm authority / Main-Council governance row:

```text
29epeLvAMyRXtpA1HaoKB1hGcAnrc1NvMCbaZ8AVRwEi
```

Owner Program / SPL Governance program:

```text
GovER5Lthms3bLBqWub97yVrMmEogzX7xNjdXpPPCVZw
```

Multi-sig / Council mint:

```text
7XenHd1YS1ae6BzUwEnDWiX1E3KR9aje5HcGdVJD4gPp
```

Confirmed Orrery program authority target:

```text
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8
```

Intended mainnet authority model:

```text
Program upgrade authority:
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8

Epoch Registry operational authority:
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8
```

The developer wallet was used only for deployment, verification, and pre-handoff metadata operations.

---

## Current Devnet State

Registry PDA:

```text
EeNBPkMCnahfjvc44qjFpSgxDyJrHZv9ASSX5fSm9crm
```

Current Epoch PDA:

```text
3gfc4aBN4goAfZj4eWKmCA9ukp4ECyw2Z93xDmXL1T4i
```

Current Epoch ID:

```text
202602
```

Status:

```text
1 = Active
```

Physis Year:

```text
2026
```

Physis Quarter:

```text
Q2
```

Calendar Year:

```text
2026
```

Calendar Quarter:

```text
Q3
```

Start Unix:

```text
1782864000
```

End Unix:

```text
1790812799
```

ASTRALIS Epoch Zero:

```text
1725148800
```

ASTRALIS Epoch Duration:

```text
21600 seconds
```

Current Devnet Authority:

```text
wfSXjyiLAv2mmCyPBhgT5ZNaPtAenNjQ6jaanQpdJJm
```

Note:

```text
Devnet authority is a developer wallet for testing only.
```

---

## Production Authority Target

```text
Physis DAO / Realms Main-Council governed authority.
```

Specifically:

```text
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8
```

Mainnet registry authority should not be a founder or developer wallet.

Mainnet registry initialization must be performed through a DAO/Realms-controlled execution path.

---

## Pending DAO Actions

The following actions remain pending:

```text
1. Transfer program upgrade authority to:
   6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8

2. Initialize the mainnet Epoch Registry through Realms.

3. Register the current Physis epoch through Realms.

4. Activate the current Physis epoch through Realms.
```

The mainnet registry should be initialized with:

```text
registry.realm =
DsWWtZrqXBcqTTPoEyFH793Euq82r95CuXWTwqo3JZur

registry.authority =
6ZuPrCK472jw3ZjRBqa6PZQ1tyVvY5BuYfWS7GMq7hX8
```

---

## Program Responsibilities

The Physis Epoch Registry is responsible for:

```text
Initializing the global Physis epoch registry
Registering quarterly Physis epochs
Activating epochs
Closing epochs
Pausing/resuming registry operations
Preserving the ASTRALIS 6-hour service epoch anchor
Exposing canonical epoch state to future Physis programs
```

The Physis Epoch Registry is not responsible for:

```text
PHY locking
Reward calculations
Reward claims
PRIVÉ eligibility
Council payments
ASTRALIS leader rotation
SmartSpot uptime
Operator emissions
Token minting
```

---

## Time Model

Canonical on-chain time:

```text
Solana Clock.unix_timestamp
```

Audit/checkpoint metadata:

```text
Solana slot
Solana epoch
```

Physis year start:

```text
April 1 UTC
```

Physis quarters:

```text
Physis Q1 = April – June
Physis Q2 = July – September
Physis Q3 = October – December
Physis Q4 = January – March
```

ASTRALIS service epoch formula:

```text
astralis_epoch_index =
floor((current_unix_timestamp - astralis_epoch_zero_ts) / astralis_epoch_duration_seconds)
```

With:

```text
astralis_epoch_zero_ts = 1725148800
astralis_epoch_duration_seconds = 21600
```

---

## Verification Commands

Local hash check:

```bash
solana-verify get-executable-hash target/deploy/physis_epoch_registry.so
```

Mainnet program hash check:

```bash
solana-verify get-program-hash \
  -u "$HELIUS_MAINNET_RPC" \
  PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE
```

Program Metadata security fetch:

```bash
npx @solana-program/program-metadata@0.7.0 fetch security \
  PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE \
  --rpc "$HELIUS_MAINNET_RPC"
```

Program Metadata IDL fetch:

```bash
npx @solana-program/program-metadata@0.7.0 fetch idl \
  PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE \
  --rpc "$HELIUS_MAINNET_RPC"
```

Traditional Anchor IDL fetch:

```bash
anchor idl fetch \
  --provider.cluster "$HELIUS_MAINNET_RPC" \
  PHYcBRWd6mKATk3xo8oYi3d55BBHUc7kAN4kK91cJoE
```
